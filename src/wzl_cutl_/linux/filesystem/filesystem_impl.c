#include "wzl_cutl/filesystem.h"

WZL_CUTL_PUBLIC(size_t) wzl_get_cwd(char* buffer, size_t buffer_size)
{
#error Unimplemented

	(void)buffer;
	(void)buffer_size;
	return 0;
}

WZL_CUTL_PUBLIC(bool)
wzl_file_exists(const char* native_path)
{
#error Unimplemented

	(void)native_path;
	return false;
}

WZL_CUTL_PUBLIC(bool) wzl_directory_exists(const char* native_path)
{
#error Unimplemented

	(void)native_path;
	return false;
}
